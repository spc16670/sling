[
    { sling, [
        { server_domain, <<"surething.co.uk">> }
        ,{ templates_dir, <<"priv/templates">> }
        ,{ dkim_txt_dns_selector, <<"surething.co.uk">> }
        ,{ email_no_reply_user, [
            { username, <<"noreply">> }
            ,{ name, <<"Drooky">> }
        ]}

        %% Adapters are tried one after another until the email is delivered
        %% successfully.
        ,{adapters,[
            { generic, [
                { protocol, <<"smtp">> }
                ,{ dns , [
                    %% PTR config
                    { ptr, <<"">> }
                    %% lookup SPF and ensure record exists for the
                    ,{ spf, <<"">> }
                    ,{ dkim, [
                        { selector, <<"asdf">> }
                        ,{ private_key, <<"">> }
                        ,{  }
                    ] }
                    ,{ dmarc, <<"">>}
                ] }
            ]}
            ,{ exchange, [
                { protocol, <<"smtp">> }
            ]}
            ,{ mailgun, [
                { protocol, <<"smtp">> }
            ]}
        ]}
    ]}
].
